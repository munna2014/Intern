---
description: Commit and push changes to GitHub
---

1. Check git status
2. Add all changes
3. Commit with a descriptive message (ask user if needed, or use a generic one if auto-running)
4. Push to the current branch

// turbo-all
1. git status
2. git add .
3. git commit -m "Update project files"
4. git push
